/**
 * @deprecated Import from `@/providers/MultiHospitalProvider` instead.
 * Compatibility re-export after enterprise folder reorganization.
 */
export {
  MultiHospitalProvider,
  useMultiHospital,
} from "@/providers/MultiHospitalProvider"
