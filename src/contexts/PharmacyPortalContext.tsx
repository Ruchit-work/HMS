/**
 * @deprecated Import from `@/providers/PharmacyPortalProvider` instead.
 * Compatibility re-export after enterprise folder reorganization.
 */
export {
  PharmacyPortalProvider,
  usePharmacyPortal,
} from "@/providers/PharmacyPortalProvider"
export type { PharmacyPortalTabId } from "@/providers/PharmacyPortalProvider"
