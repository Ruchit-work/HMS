/**
 * Domain feature modules (UI + feature-local hooks).
 * App routes under `src/app` import from here; keep pages thin.
 *
 * Layout:
 * - admin/        tabs, components, hq, chrome
 * - receptionist/ tabs, components, hooks
 * - doctor/       clinical, appointments, anatomy, schedule
 * - patient/      booking, appointments UI
 * - pharmacy/     portal shell, management, ops UI
 * - documents/, forms/, auth/, billing/, payments/, consent/, prescription/
 */
